# PageSage Development Environment - Homebrew Dependencies
#
# Install all dependencies with: brew bundle install
# Update this file when adding new system dependencies

# Core Development Tools
brew "node"              # JavaScript runtime for SvelteKit
brew "gh"                # GitHub CLI for authentication and operations
brew "git"               # Version control

# MCP Server Dependencies
brew "uv"                # Fast Python package manager (for Python-based MCP servers)
brew "deno"              # Alternative JavaScript runtime (some MCP servers use this)

# Optional: Terminal Enhancements
brew "eza"               # Modern ls replacement with icons and colors
brew "zoxide"            # Smart directory navigation (z command)
brew "autojump"          # Directory jumping (j command)

# Optional: Additional Development Tools
# Uncomment if needed for your workflow
# brew "wget"            # Download files from terminal
# brew "jq"              # JSON processor
# brew "yq"              # YAML processor

# Note: The following are already installed on your system
# but are not strictly required for PageSage core development:
# - ffmpeg, tesseract, imagemagick, etc. (keep if you use them)
